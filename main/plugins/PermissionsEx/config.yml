multiserver:
  use-netevents: true
permissions:
  debug: false
  allowOps: false
  user-add-groups-last: false
  log-players: false
  createUserRecords: false
  backend: sql
  informplayers:
    changes: false
  basedir: plugins/PermissionsEx
  backends:
    sql:
      uri: mysql://${MYSQL_HOST}:${MYSQL_PORT}/permissionsex
      user: ${MYSQL_USER}
      password: ${MYSQL_PASSWORD}
      type: sql
updater: true
alwaysUpdate: false
